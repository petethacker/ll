package main

func todo() {
	print("change RemoveArgs() for the better (would have helped if i said how when i originally added this todo")
	print("cleanup ... lots")
	print("recurse directories functionallity partly done. doesnt do symlinks and needs better formatting")
	print("Arg to stop the filelist ordering")
}
